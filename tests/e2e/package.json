{
  "name": "blazor-apexgantt-e2e",
  "private": true,
  "type": "module",
  "description": "Headless browser smoke + interaction test for the Blazor-ApexGantt sample. Guards against interop-only regressions (a chart that builds and serializes cleanly but fails to render or wire up events in the browser) whenever the apexgantt core is bumped.",
  "scripts": {
    "test": "node smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
